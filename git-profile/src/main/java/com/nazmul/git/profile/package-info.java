/**
 * Using strategy pattern to choose an avatar for a GitHub user.
 *
 * Intent
 * a) Separate superclass from sublass
 * b) Capture abstraction in an interface
 * c) Define a family of algorithms, encapsulate each one, and make them interchangeable
 *
 *
 */

package com.nazmul.git.profile;
